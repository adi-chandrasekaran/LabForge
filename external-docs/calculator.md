# Calculator

The calculator is used for lab math such as molarity, dilution, mass, volume, Beer-Lambert concentration, and yield.

## Current Behavior

The prototype includes the calculator UI. Backend persistence does not depend on calculator output yet.

## Planned Inline Use

When editing workflow or experiment values, a `#` calculator control will open the same calculator as a popup. A calculated result can be inserted into the selected field.

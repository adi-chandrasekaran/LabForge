# Testing Strategy

Every PR must include backend/API tests and a Playwright demo test.

Backend tests verify schemas, persistence, permissions, and route behavior. Playwright tests should be written as runnable demos that can be stepped through with traces.
